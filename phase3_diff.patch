diff --git a/CMakeLists.txt b/CMakeLists.txt
index 90554e3..e3c29c9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -9,6 +9,9 @@ set(CMAKE_CXX_EXTENSIONS OFF)
 # Add include directory
 include_directories(include)
 
+# Build options
+option(HELIX_NATIVE_BUILD "Enable -march=native and LTO for local performance builds" OFF)
+
 # Define the HELIX core library
 add_library(helix 
     src/core/allocator.cpp
@@ -25,6 +28,7 @@ add_library(helix
     src/backend/matmul/avx2_micro_matmul.cpp
     src/backend/matmul/openmp_matmul.cpp
     src/backend/matmul/matmul_backend.cpp
+    src/backend/simd/simd_backend.cpp
     src/autograd/autograd_meta.cpp
     src/autograd/function.cpp
     src/autograd/graph_builder.cpp
@@ -51,7 +55,11 @@ if(CMAKE_SYSTEM_PROCESSOR MATCHES "amd64|AMD64|x86_64|X86_64")
     if(MSVC)
         target_compile_options(helix PUBLIC /arch:AVX2 /O2)
     else()
-        target_compile_options(helix PUBLIC -mavx2 -mfma -O3)
+        if(HELIX_NATIVE_BUILD)
+            target_compile_options(helix PUBLIC -march=native -O3 -flto)
+        else()
+            target_compile_options(helix PUBLIC -mavx2 -mfma -O3)
+        endif()
     endif()
 else()
     if(MSVC)
diff --git a/benchmark/matmul_benchmark.cpp b/benchmark/matmul_benchmark.cpp
index bcd4e72..ce4ca8a 100644
--- a/benchmark/matmul_benchmark.cpp
+++ b/benchmark/matmul_benchmark.cpp
@@ -34,6 +34,7 @@ static std::vector<BenchmarkResult> run_matmul_benchmark(size_t size) {
     // Công thức tính số phép toán dấu phẩy động thống nhất: 2 * M * K * N
     double ops = 2.0 * M * K * N;
 
+    // Strategies to benchmark
     // Strategies to benchmark
     std::vector<std::pair<MatMulStrategy, std::string>> strategies = {
         {MatMulStrategy::Naive, "Naive"}, 
diff --git a/include/core/dispatcher.hpp b/include/core/dispatcher.hpp
index b73a5a8..c5c6eb8 100644
--- a/include/core/dispatcher.hpp
+++ b/include/core/dispatcher.hpp
@@ -18,6 +18,7 @@ namespace helix {
 
         // Mathematical Operations
         static Tensor add(const Tensor& a, const Tensor& b);
+        static void add_(Tensor& a, const Tensor& b);
         static Tensor sub(const Tensor& a, const Tensor& b);
         static Tensor mul(const Tensor& a, const Tensor& b);
         static Tensor div(const Tensor& a, const Tensor& b);
diff --git a/include/core/tensor.hpp b/include/core/tensor.hpp
index da31634..f0e572d 100644
--- a/include/core/tensor.hpp
+++ b/include/core/tensor.hpp
@@ -67,6 +67,7 @@ namespace helix {
         Tensor operator-(const Tensor& other) const;
         Tensor operator*(const Tensor& other) const;
         Tensor operator/(const Tensor& other) const;
+        Tensor& add_(const Tensor& other);
 
         // Scalar arithmetic
         Tensor operator+(float scalar) const;
@@ -102,6 +103,7 @@ namespace helix {
         void zero_();
         Tensor contiguous() const;
         bool is_contiguous() const;
+        bool is_shared() const;
 
     private:
         // Internal constructor used for views and other internals
diff --git a/src/autograd/engine.cpp b/src/autograd/engine.cpp
index 7b33c1e..1cd6d36 100644
--- a/src/autograd/engine.cpp
+++ b/src/autograd/engine.cpp
@@ -20,7 +20,11 @@ namespace helix {
             if (!meta_->has_grad()) {
                 meta_->set_grad(grad_outputs[0]);
             } else {
-                meta_->set_grad(meta_->grad() + grad_outputs[0]);
+                if (!meta_->grad().is_shared() && meta_->grad().shape() == grad_outputs[0].shape()) {
+                    meta_->grad().add_(grad_outputs[0]);
+                } else {
+                    meta_->set_grad(meta_->grad() + grad_outputs[0]);
+                }
             }
             return {};
         }
@@ -136,8 +140,12 @@ namespace helix {
                     if (!node_gradients.contains(next)) {
                         node_gradients[next] = {input_grads[i]};
                     } else {
-                        // Assuming single output per node
-                        node_gradients[next][0] = node_gradients[next][0] + input_grads[i];
+                        // Inplace gradient accumulation if safe
+                        if (!node_gradients[next][0].is_shared() && node_gradients[next][0].shape() == input_grads[i].shape()) {
+                            node_gradients[next][0].add_(input_grads[i]);
+                        } else {
+                            node_gradients[next][0] = node_gradients[next][0] + input_grads[i];
+                        }
                     }
 
                     in_degrees[next]--;
diff --git a/src/backend/matmul/matmul_config.hpp b/src/backend/matmul/matmul_config.hpp
index c239e05..954b97d 100644
--- a/src/backend/matmul/matmul_config.hpp
+++ b/src/backend/matmul/matmul_config.hpp
@@ -2,6 +2,6 @@
 #include <cstddef>
 namespace helix {
     struct MatMulConfig {
-        static constexpr size_t block_size = 64;
+        static constexpr size_t block_size = 256;
     };
 }  // namespace helix
diff --git a/src/backend/matmul/openmp_matmul.cpp b/src/backend/matmul/openmp_matmul.cpp
index d0c17bb..818be43 100644
--- a/src/backend/matmul/openmp_matmul.cpp
+++ b/src/backend/matmul/openmp_matmul.cpp
@@ -30,7 +30,7 @@ namespace helix {
         std::fill_n(out, M * N, 0.0f);
         constexpr size_t BLOCK = MatMulConfig::block_size;
 
-#pragma omp parallel for collapse(2) schedule(static)
+#pragma omp parallel for schedule(dynamic)
         for (int ih = 0; ih < static_cast<int>(M); ih += static_cast<int>(BLOCK)) {
             for (int jh = 0; jh < static_cast<int>(N); jh += static_cast<int>(BLOCK)) {
                 const size_t i_end = std::min(static_cast<size_t>(ih) + BLOCK, M);
diff --git a/src/core/dispatcher.cpp b/src/core/dispatcher.cpp
index c683185..a67c97d 100644
--- a/src/core/dispatcher.cpp
+++ b/src/core/dispatcher.cpp
@@ -3,6 +3,7 @@
 #include <stdexcept>
 
 #include "backend/cpu_backend.hpp"
+#include "backend/simd_backend.hpp"
 #include "core/broadcast.hpp"
 #include "core/graph_builder.hpp"
 #include "core/tensor.hpp"
@@ -26,9 +27,10 @@ namespace helix {
         Tensor lhs = ensure_contiguous(a.broadcast_to(out_shape));
         Tensor rhs = ensure_contiguous(b.broadcast_to(out_shape));
         Tensor out(out_shape, a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::add(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::add(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+            else CPUBackend::add(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         if (g_graph_builder) {
             g_graph_builder->build(OperationContext{OpCategory::Binary, OpType::Add, out, {a, b}});
@@ -36,14 +38,32 @@ namespace helix {
         return out;
     }
 
+    void Dispatcher::add_(Tensor& a, const Tensor& b) {
+        if (a.shape() != b.shape()) {
+            throw std::invalid_argument("Inplace addition requires matching shapes without broadcasting.");
+        }
+        Tensor lhs = ensure_contiguous(a);
+        Tensor rhs = ensure_contiguous(b);
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::add(lhs.data_ptr(), rhs.data_ptr(), lhs.data_ptr(), lhs.numel());
+            else CPUBackend::add(lhs.data_ptr(), rhs.data_ptr(), lhs.data_ptr(), lhs.numel());
+        } else
+            throw std::runtime_error("Unsupported device");
+        
+        if (!a.is_contiguous()) {
+            a.copy_(lhs);
+        }
+    }
+
     Tensor Dispatcher::sub(const Tensor& a, const Tensor& b) {
         const Shape out_shape = compute_broadcast_shape(a.shape(), b.shape());
         Tensor lhs = ensure_contiguous(a.broadcast_to(out_shape));
         Tensor rhs = ensure_contiguous(b.broadcast_to(out_shape));
         Tensor out(out_shape, a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::sub(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::sub(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+            else CPUBackend::sub(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         if (g_graph_builder) {
             g_graph_builder->build(OperationContext{OpCategory::Binary, OpType::Sub, out, {a, b}});
@@ -56,9 +76,10 @@ namespace helix {
         Tensor lhs = ensure_contiguous(a.broadcast_to(out_shape));
         Tensor rhs = ensure_contiguous(b.broadcast_to(out_shape));
         Tensor out(out_shape, a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::mul(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::mul(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+            else CPUBackend::mul(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         if (g_graph_builder) {
             g_graph_builder->build(OperationContext{OpCategory::Binary, OpType::Mul, out, {a, b}});
@@ -71,9 +92,10 @@ namespace helix {
         Tensor lhs = ensure_contiguous(a.broadcast_to(out_shape));
         Tensor rhs = ensure_contiguous(b.broadcast_to(out_shape));
         Tensor out(out_shape, a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::div(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::div(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+            else CPUBackend::div(lhs.data_ptr(), rhs.data_ptr(), out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         if (g_graph_builder) {
             g_graph_builder->build(OperationContext{OpCategory::Binary, OpType::Div, out, {a, b}});
@@ -84,9 +106,10 @@ namespace helix {
     Tensor Dispatcher::add_scalar(const Tensor& a, float scalar) {
         Tensor lhs = ensure_contiguous(a);
         Tensor out(a.shape(), a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::add_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::add_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+            else CPUBackend::add_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         // We omit graph builder for scalar ops for now, as they are primarily used in backward passes
         // where inputs don't require gradients.
@@ -96,9 +119,10 @@ namespace helix {
     Tensor Dispatcher::sub_scalar(const Tensor& a, float scalar) {
         Tensor lhs = ensure_contiguous(a);
         Tensor out(a.shape(), a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::sub_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::sub_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+            else CPUBackend::sub_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         return out;
     }
@@ -106,9 +130,10 @@ namespace helix {
     Tensor Dispatcher::mul_scalar(const Tensor& a, float scalar) {
         Tensor lhs = ensure_contiguous(a);
         Tensor out(a.shape(), a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::mul_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::mul_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+            else CPUBackend::mul_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         return out;
     }
@@ -116,9 +141,10 @@ namespace helix {
     Tensor Dispatcher::div_scalar(const Tensor& a, float scalar) {
         Tensor lhs = ensure_contiguous(a);
         Tensor out(a.shape(), a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::div_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::div_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+            else CPUBackend::div_scalar(lhs.data_ptr(), scalar, out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         return out;
     }
@@ -126,9 +152,10 @@ namespace helix {
     Tensor Dispatcher::neg(const Tensor& a) {
         Tensor lhs = ensure_contiguous(a);
         Tensor out(a.shape(), a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::neg(lhs.data_ptr(), out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::neg(lhs.data_ptr(), out.data_ptr(), out.numel());
+            else CPUBackend::neg(lhs.data_ptr(), out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         if (g_graph_builder) {
             g_graph_builder->build(OperationContext{OpCategory::Unary, OpType::Neg, out, {a}});
@@ -178,9 +205,10 @@ namespace helix {
     Tensor Dispatcher::relu(const Tensor& a) {
         Tensor lhs = ensure_contiguous(a);
         Tensor out(a.shape(), a.dtype(), a.device());
-        if (a.device().is_cpu())
-            CPUBackend::relu(lhs.data_ptr(), out.data_ptr(), out.numel());
-        else
+        if (a.device().is_cpu()) {
+            if (SIMDBackend::is_supported()) SIMDBackend::relu(lhs.data_ptr(), out.data_ptr(), out.numel());
+            else CPUBackend::relu(lhs.data_ptr(), out.data_ptr(), out.numel());
+        } else
             throw std::runtime_error("Unsupported device");
         if (g_graph_builder) {
             g_graph_builder->build(OperationContext{OpCategory::Unary, OpType::ReLU, out, {a}});
@@ -252,7 +280,8 @@ namespace helix {
             const Shape out_shape = keepdim ? Shape(std::vector<size_t>(a.rank(), 1)) : Shape();
             Tensor out(out_shape, a.dtype(), a.device());
             if (a.device().is_cpu()) {
-                CPUBackend::sum(lhs.data_ptr(), out.data_ptr(), 1, a.numel(), 1);
+                if (SIMDBackend::is_supported()) SIMDBackend::sum(lhs.data_ptr(), out.data_ptr(), 1, a.numel(), 1);
+                else CPUBackend::sum(lhs.data_ptr(), out.data_ptr(), 1, a.numel(), 1);
             } else {
                 throw std::runtime_error("Unsupported device");
             }
@@ -286,7 +315,8 @@ namespace helix {
         for (size_t i = dim + 1; i < a.rank(); ++i) inner_size *= a.shape()[i];
 
         if (a.device().is_cpu()) {
-            CPUBackend::sum(lhs.data_ptr(), out.data_ptr(), outer_size, dim_size, inner_size);
+            if (SIMDBackend::is_supported()) SIMDBackend::sum(lhs.data_ptr(), out.data_ptr(), outer_size, dim_size, inner_size);
+            else CPUBackend::sum(lhs.data_ptr(), out.data_ptr(), outer_size, dim_size, inner_size);
         } else {
             throw std::runtime_error("Unsupported device");
         }
@@ -306,7 +336,8 @@ namespace helix {
             const Shape out_shape = keepdim ? Shape(std::vector<size_t>(a.rank(), 1)) : Shape();
             Tensor out(out_shape, a.dtype(), a.device());
             if (a.device().is_cpu()) {
-                CPUBackend::mean(lhs.data_ptr(), out.data_ptr(), 1, a.numel(), 1);
+                if (SIMDBackend::is_supported()) SIMDBackend::mean(lhs.data_ptr(), out.data_ptr(), 1, a.numel(), 1);
+                else CPUBackend::mean(lhs.data_ptr(), out.data_ptr(), 1, a.numel(), 1);
             } else {
                 throw std::runtime_error("Unsupported device");
             }
@@ -340,7 +371,8 @@ namespace helix {
         for (size_t i = dim + 1; i < a.rank(); ++i) inner_size *= a.shape()[i];
 
         if (a.device().is_cpu()) {
-            CPUBackend::mean(lhs.data_ptr(), out.data_ptr(), outer_size, dim_size, inner_size);
+            if (SIMDBackend::is_supported()) SIMDBackend::mean(lhs.data_ptr(), out.data_ptr(), outer_size, dim_size, inner_size);
+            else CPUBackend::mean(lhs.data_ptr(), out.data_ptr(), outer_size, dim_size, inner_size);
         } else {
             throw std::runtime_error("Unsupported device");
         }
diff --git a/src/core/tensor.cpp b/src/core/tensor.cpp
index 48a6068..c79642f 100644
--- a/src/core/tensor.cpp
+++ b/src/core/tensor.cpp
@@ -84,6 +84,10 @@ namespace helix {
 
     bool Tensor::is_contiguous() const { return impl_->is_contiguous(); }
 
+    bool Tensor::is_shared() const {
+        return impl_.use_count() > 1 || impl_->storage().use_count() > 1;
+    }
+
     Tensor Tensor::view(Shape new_shape) const {
         if (new_shape.numel() != numel()) {
             throw std::invalid_argument("view shape must have the same number of elements");
@@ -241,6 +245,10 @@ namespace helix {
     }
 
     Tensor Tensor::operator+(const Tensor& other) const { return Dispatcher::add(*this, other); }
+    Tensor& Tensor::add_(const Tensor& other) {
+        Dispatcher::add_(*this, other);
+        return *this;
+    }
     Tensor Tensor::operator-(const Tensor& other) const { return Dispatcher::sub(*this, other); }
     Tensor Tensor::operator*(const Tensor& other) const { return Dispatcher::mul(*this, other); }
     Tensor Tensor::operator/(const Tensor& other) const { return Dispatcher::div(*this, other); }
